specVersion: "5.0"
metadata:
  name: application.a
type: application
builder:
  customTasks:
    - name: ui5-tooling-stringreplace-task
      afterTask: replaceVersion
      configuration:
        files:
          - "**/*.js"
        replace:
          - placeholder: ${PLACEHOLDER_TEXT}
            value: "'INSERTED_TEXT'"
